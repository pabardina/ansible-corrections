mysql_root_password: root
mysql_databases:
  - name: gogs
    encoding: latin1
    collation: latin1_general_ci
mysql_users:
  - name: gogs
    host: "%"
    password: gogs
    priv: "gogs.*:ALL"

gogs_use_mysql: true
gogs_db_name: gogs
gogs_db_username: gogs
gogs_db_password: gogs
gogs_http_port: "3000"
